{
  "dependencies": {},
  "devDependencies": {
    "normalize.css": "^8.0.1",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.3",
    "swiper": "^5.3.6"
  },
  "scripts": {
    "dev": "parcel index.html --open",
    "build": "rmdir dist /s /q && parcel build index.html"
  }
}
